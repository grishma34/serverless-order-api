# Serverless Order API

![coverage](docs/assets/coverage.svg)
&nbsp;`python3.14` · `arm64` Lambda · DynamoDB single-table · AWS SAM

<!-- The badge is generated from this repository's own coverage run
     (docs/evidence/make_badge.py) rather than fetched from a badge service:
     nothing here should depend on a third party staying up, or report a number
     some CI provider last cached. -->

A serverless REST API for customer orders on AWS. Built to demonstrate production
patterns rather than feature count: a single-table DynamoDB design with zero
scans, idempotent writes that survive Lambda retries, one-domain delivery through
CloudFront, and a deploy pipeline with no long-lived credentials.

> **Status — built, tested, CI green; never deployed.**
> All seven phases of `PLAN.md` are implemented. 603 tests pass at 100% coverage,
> `sam validate --lint` and `sam build` both succeed, and CI is green on `main`.
> **No AWS resources exist.** The deploy job skips until an account is
> bootstrapped, so nothing here has run against real DynamoDB, IAM or CloudFront.
> What that leaves unverified is listed under
> [Not yet proven](#not-yet-proven) — it is the honest boundary of this project,
> not a footnote.

## Architecture

```
                        ┌──────────────────────────────────────────────┐
   Browser ───────────► │                 CloudFront                   │
                        │        one domain for UI and API             │
                        └───────┬──────────────────────────┬───────────┘
                        default │                          │ /api/*
                                ▼                          ▼
                     ┌────────────────────┐    ┌───────────────────────┐
                     │  S3 (frontend)     │    │ API Gateway (HTTP API)│
                     │  private, OAC-only │    └──────────┬────────────┘
                     └────────────────────┘               │
                                                          ▼
                                   ┌──────────────────────────────────────┐
                                   │  5 Lambdas (python3.14, arm64)       │
                                   │  create · get · list×2 · update      │
                                   │  one scoped IAM policy each          │
                                   └──────────────────┬───────────────────┘
                                                      ▼
                                   ┌──────────────────────────────────────┐
                                   │  DynamoDB — 1 table, 2 sparse GSIs,  │
                                   │  TTL, no scans                       │
                                   └──────────────────────────────────────┘

   GitHub ── merge to main ──► Actions ── OIDC ──► sam build && sam deploy
```

Because the UI and the API share a CloudFront domain, the browser makes only
same-origin requests — there is no CORS configuration anywhere in this project,
which is a design outcome rather than an omission.

## What it demonstrates, and what proves it

Each claim below names the test that backs it. Captured output for the first
three is in [`docs/EVIDENCE.md`](docs/EVIDENCE.md), regenerated by
`bash docs/evidence/capture.sh`.

| Claim | Proof |
|---|---|
| **No table scans**, ever (REQ-0012) | `tests/unit/data/test_no_scan.py` — a static grep over `src/` *and* a botocore call log of the operations actually issued. A meta-test fires a real `Scan` so the detector can't pass vacuously. Enforced in IAM too: no function policy grants `dynamodb:Scan`. |
| **A retry cannot create a duplicate** (REQ-0010) | `TestIdempotentCreate` + `TestReplaySemantics` — one `TransactWriteItems` with condition expressions on both the idempotency record and the order. The second create raises, leaves exactly one order, and writes no rows at all. |
| **Indexes can't disagree with the item** (REQ-0011) | `transition_status` rewrites `status`, `GSI1SK` and `GSI2PK` in one conditional `UpdateItem` pinned to the expected current status. |
| **6 documented access patterns, all `Query`/`GetItem`** | One test per pattern in `test_order_repository.py`; every query maps to a row in `docs/DYNAMODB_DESIGN.md`. |
| **Complete state machine** (REQ-0006/0007) | All 25 ordered status pairs asserted, plus reachability and acyclicity — not a sample. |
| **Infrastructure is checked, not just linted** | `tests/unit/infra/` asserts the template matches the design doc, no policy grants `Scan` or uses a `*` resource, no workflow reads a static AWS key, and the OIDC trust is pinned to one repo and branch. |
| **The frontend can't drift from the server** | A test parses the UI's transition map out of `app.js` and compares it to `services/status_machine.py`. Another fails the build if an absolute API origin ever appears in `frontend/`. |

## Test suite

603 tests, 100% coverage of `src/` (596 statements, 122 branches), gate at 90%.
No test touches AWS: an autouse fixture plants unusable credentials so an escaped
mock fails on authentication rather than reaching a real account (NFR-0002).

| Layer | Tests | AWS |
|---|---|---|
| `tests/unit/shared/` | 51 | none |
| `tests/unit/data/` | 71 | moto |
| `tests/unit/services/` | 232 | none — in-memory fake repository |
| `tests/unit/handlers/` | 108 | moto, end-to-end through the real stack |
| `tests/unit/infra/` | 141 | none — templates, workflows and docs read as data |

The service layer runs against a fake repository, which is only sound while the
fake fails the same way the real one does — so `test_fake_matches_repository.py`
runs the same scenarios against both the fake and moto.

## Running it

```bash
python3.14 -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt   # pulls in src/requirements.txt too

# The quality gate — what CI runs
ruff check src tests && ruff format --check src tests
pytest --cov=src --cov-report=term-missing --cov-fail-under=90

# Infrastructure
sam validate --lint
sam build          # needs the venv active: system python3.14 has no pip
```

Deploying is documented in [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md) and has not
been done.

## API

Base path `/api`, routed by CloudFront to API Gateway. Full detail in
[`docs/API_SPEC.md`](docs/API_SPEC.md).

| Method | Path | Purpose |
|---|---|---|
| `POST` | `/api/orders` | Create — `Idempotency-Key` required; `201`, or `200` on replay |
| `GET` | `/api/orders/{orderId}` | Fetch one order with its line items |
| `GET` | `/api/customers/{customerId}/orders` | A customer's orders; `?status=` filters |
| `GET` | `/api/orders?status=X` | Ops listing across customers |
| `PATCH` | `/api/orders/{orderId}` | Status transition; `409` if illegal |

```
PLACED ──► PAID ──► SHIPPED ──► DELIVERED
   │         │
   └────┬────┘
        ▼
    CANCELLED          (terminal; DELIVERED also terminal)
```

## Not yet proven

Everything above is verified locally. These need a real deployment, and the
checklist for them is [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md) § 5:

- **moto vs real DynamoDB.** `PLAN.md` § Risks flags `TransactWriteItems`
  condition semantics as the top unknown. It is the mechanism behind REQ-0010,
  so the idempotency guarantee is proven against moto's imitation of DynamoDB,
  not DynamoDB.
- **GSI-only IAM scoping.** The two list functions are granted `dynamodb:Query`
  on their index ARN and not the table. I believe that is sufficient; a deploy
  would confirm it.
- **CloudFront path handling.** Routes carry the `/api` prefix and the stage is
  `$default`, so `/api/*` should arrive unrewritten.
- **The AWS half of the pipeline.** `deploy.yml`'s test job runs green on every
  push, but the deploy job skips until `AWS_DEPLOY_ROLE_ARN` is set — so build,
  deploy, S3 sync and invalidation have never executed, and neither has the OIDC
  bootstrap stack.
- **Branch protection (REQ-0024).** Not applied. Documented in
  `docs/DEPLOYMENT.md` § 3, with the caveat that requiring an approving review
  on a single-maintainer repository blocks all merges.

`TASKS.md` carries the same list against the individual checkboxes.

### Post-deploy smoke checklist

Not yet run. Full commands in [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md) § 5;
this is what they cover and why each belongs here rather than in the suite.

| # | Check | Expected | Why it can't be a unit test |
|---|---|---|---|
| 1 | `POST /api/orders` with a fresh `Idempotency-Key` | `201` + order body | — |
| 2 | Repeat **the same key** | `200`, byte-identical body, still one order | The REQ-0010 proof against real condition expressions |
| 3 | `GET /api/customers/{id}/orders` | Exactly one order | — |
| 4 | `PATCH` to `PAID` | `200` | Confirms GSI-only IAM scoping suffices for an index `Query` |
| 5 | `PATCH` `SHIPPED → CANCELLED` | `409` with `from`/`to` | — |
| 6 | Direct S3 object URL | `403` | OAC is enforced by CloudFront and S3, which moto does not exercise |
| 7 | Load the CloudFront root | UI renders, `/api/*` succeeds | Confirms `/api/*` arrives unrewritten |

The frontend mints an idempotency key per submission and **reuses it when a
submission fails**, so check 2 runs straight from the UI via the "Resend with the
same key" button.

## Documentation map

| Doc | Contents |
|---|---|
| [`PLAN.md`](PLAN.md) | Phased build plan, risks, sizing |
| [`TASKS.md`](tasks.md) | Executable checklist, with what remains open |
| [`CLAUDE.md`](claude.md) | AI-agent operating rules and guardrails |
| [`docs/REQUIREMENTS.md`](docs/REQUIREMENTS.md) | REQ-####/NFR-#### register |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | System diagram, key decisions |
| [`docs/DYNAMODB_DESIGN.md`](docs/DYNAMODB_DESIGN.md) | Access patterns, key design, write integrity |
| [`docs/API_SPEC.md`](docs/API_SPEC.md) | Endpoints, payloads, state machine, errors |
| [`docs/TEST_STRATEGY.md`](docs/TEST_STRATEGY.md) | Test layers, fixtures, coverage policy |
| [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md) | Bootstrap, OIDC, branch protection, smoke checklist |
| [`docs/EVIDENCE.md`](docs/EVIDENCE.md) | Captured test output behind the claims above |

## Future work (deliberately out of scope)

Cognito auth, payment integration, multi-region/DR, load testing.
